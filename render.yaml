services:
  - type: web
    name: ai-data-analyst-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python -m database.seed_data
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false